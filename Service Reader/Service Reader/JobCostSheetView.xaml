<UserControl x:Class="Service_Reader.JobCostSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Service_Reader"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <DataTemplate x:Key="serviceSubmissionTemplate" DataType="{x:Type local:oldServiceSubmissionModel}">
            <local:ServiceSubmissionDetails DataContext="{Binding}" />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.DataContext>
            <local:JobCostSheetViewModel/>
        </Grid.DataContext>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:CanvasUserView x:Name="ucUserDetails" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4"/>
        <DatePicker x:Name="dtSubmissionsFrom" SelectedDate="{Binding FromDate}" Grid.Column="1" Grid.Row="1"/>
        <Label x:Name="label" Content="From" Grid.Column="0" Grid.Row="1"/>
        <DatePicker x:Name="dtSubmissionsTo" SelectedDate="{Binding ToDate}" Grid.Row="1" Grid.Column="3"/>
        <Label x:Name="labelTo" Content="To" Grid.Column="2" Grid.Row="1"/>
        <Button Content="Load CSV" Command="{Binding loadCsv}" Grid.Column="0" Grid.Row="2" Margin="5" />
        <Button Content="Create Cost Sheet" Command="{Binding createCostSheet}" Grid.Column="1" Grid.Row="2" Margin="5" />
        <local:CustomDataGrid x:Name="dataGrid" ItemsSource="{Binding AllServiceSubmissions}" AutoGenerateColumns="False" Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="6" Margin="5" SelectedItemsList="{Binding Path=SelectedSubmissions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            <local:CustomDataGrid.Columns>
                <DataGridTextColumn Header="Submission No" Binding="{Binding SubmissionNo}"/>
                <DataGridTextColumn Header="Engineer" Binding="{Binding EngineerFullName}"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer}"/>
                <DataGridTextColumn Header="Job Start" Binding="{Binding JobStart, StringFormat=\{0:dd/MM/yy\}}"/>
            </local:CustomDataGrid.Columns>
            <local:CustomDataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Approved}" Value="True">
                            <Setter Property="Background" Value="LightGreen"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Approved}" Value="False">
                            <Setter Property="Background" Value="LightCyan"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </local:CustomDataGrid.RowStyle>
        </local:CustomDataGrid>
        </Grid>
</UserControl>
